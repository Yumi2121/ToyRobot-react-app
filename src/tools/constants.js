export const placeCommandMatch = /^place\(\d\s?,\s?\d,\s?'?(SOUTH|NORTH|WEST|EAST)'?\)$/;

export const moveCommandMatch = /^move\(\)$/;

export const leftCommandMatch = /^left\(\)$/;

export const rightCommandMatch = /^right\(\)$/;

export const reportCommandMatch = /^report\(\)$/;

export const gridSize = 5;

